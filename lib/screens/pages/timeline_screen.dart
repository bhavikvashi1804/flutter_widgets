import 'package:flutter/material.dart';


class TimelineScreenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Timeline'),
      ),
      body: Center(
        child: Text(
          'Your Timeline comes here.'
        ),
      ),
      
    );
  }
}